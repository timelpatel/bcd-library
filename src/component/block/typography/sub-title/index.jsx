import React from 'react'
import styles from './style.scss'

const Typography__Sub_Title = (props) => (
  <h1 className={styles.typography__sub_title}>
    {props.text}
  </h1>
)

export default Typography__Sub_Title
