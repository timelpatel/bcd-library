import React from 'react'
import styles from './style'

const Typography__Logo = (props) => (
  <h1 className={styles.typography__logo}>
    {props.children}
  </h1>
)

export default Typography__Logo
